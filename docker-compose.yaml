version: '2'

services:
  shop:
      image: "romas/shop:latest"
      ports:
        - 9000:9000
      depends_on:
            - warehouse
  warehouse:
    image: "romas/warehouse:latest"
    ports:
      - 9001:9001